# Regenerated by `make lookbook` (inlines ChordFont TTF)
lookbook.html

# Regenerated by `make lookbook-pdf`
lookbook.pdf

# Agent capture screenshots (local iteration only)
captures/

# Temporary dirs when re-importing agent corpus zips
harvested/
rendered/
